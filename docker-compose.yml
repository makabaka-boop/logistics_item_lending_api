version: '3.8'
services:
  logistics-api:
    build: .
    container_name: logistics-item-lending-api
    ports:
      - "8011:8011"
    volumes:
      - logistics-data:/data
    restart: unless-stopped

volumes:
  logistics-data:
