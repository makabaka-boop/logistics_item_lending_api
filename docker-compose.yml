version: '3.8'

services:
  logistics-api:
    build: .
    container_name: logistics-item-lending
    ports:
      - "8011:8011"
    volumes:
      - h2-data:/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai

volumes:
  h2-data:
